-- MySQL Workbench Synchronization
-- Generated: 2023-10-08 23:26
-- Model: New Model
-- Version: 1.0
-- Project: Name of the project
-- Author: Dani

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION';

CREATE SCHEMA IF NOT EXISTS `suprimed` DEFAULT CHARACTER SET utf8 ;

CREATE TABLE IF NOT EXISTS `suprimed`.`Pacientes` (
  `ID_Pac` INT(11) NOT NULL AUTO_INCREMENT,
  `Nome_Pac` VARCHAR(20) NOT NULL,
  `Email` VARCHAR(20) NULL DEFAULT NULL,
  `Telefone` BINARY(14) NOT NULL,
  `Data_Nasc` DATE NOT NULL,
  PRIMARY KEY (`ID_Pac`),
  UNIQUE INDEX `Email_UNIQUE` (`Email` ASC) VISIBLE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8;

CREATE TABLE IF NOT EXISTS `suprimed`.`Instrutores` (
  `ID_Inst` INT(11) NOT NULL AUTO_INCREMENT,
  `Nome_Inst` VARCHAR(45) NOT NULL,
  `Email` VARCHAR(45) NULL DEFAULT NULL,
  `Telefone` BINARY(14) NOT NULL,
  `Data_Nasc` DATE NOT NULL,
  PRIMARY KEY (`ID_Inst`),
  UNIQUE INDEX `Email_UNIQUE` (`Email` ASC) VISIBLE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8;

CREATE TABLE IF NOT EXISTS `suprimed`.`Aula` (
  `ID_Aula` INT(11) NOT NULL AUTO_INCREMENT,
  `Nome_Aula` VARCHAR(20) NOT NULL,
  `Horário` TIME NOT NULL,
  `Data` DATE NOT NULL,
  PRIMARY KEY (`ID_Aula`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8;

CREATE TABLE IF NOT EXISTS `suprimed`.`Agenda_Aula` (
  `ID_Agenda` TIMESTAMP NOT NULL,
  `ID_Pac` TIMESTAMP NOT NULL,
  `ID_Inst` VARCHAR(45) NOT NULL,
  `ID_Aula` VARCHAR(45) NOT NULL,
  `Datetime_Aula` DATETIME NOT NULL,
  PRIMARY KEY (`ID_Agenda`),
  CONSTRAINT `ID_Pac`
    FOREIGN KEY ()
    REFERENCES `suprimed`.`Pacientes` ()
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `ID_Inst`
    FOREIGN KEY ()
    REFERENCES `suprimed`.`Instrutores` ()
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `ID_Aula`
    FOREIGN KEY ()
    REFERENCES `suprimed`.`Aula` ()
    ON DELETE RESTRICT
    ON UPDATE RESTRICT)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8;


SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;

